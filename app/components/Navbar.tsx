"use client";

// Simplified Navbar - Header component now handles main navigation
// This component is kept for backward compatibility but can be removed if not needed
export default function Navbar() {
  return null;
}
